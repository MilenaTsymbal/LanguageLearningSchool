@model Lesson

<h5>Are you sure you want to delete this lesson?</h5>
<div class="form-group">
    <form method="post" asp-controller="Lesson" asp-action="Delete" asp-route-lessonId="@Model.LessonId">
        @*<input asp-for="CourseId" type="hidden" class="form-control">
        <input asp-for="LessonId" type="hidden" class="form-control">
        <div class="form-group">
            <label asp-for="LessonName">Lesson name</label>
            <input asp-for="LessonName" type="text" class="form-control" placeholder="Enter lesson name">
            <span asp-validation-for="LessonName" class="text-danger"></span>
        </div>
        <div class="form-group">
            <label asp-for="Material">Material</label>
            <input asp-for="Material" type="text" class="form-control" id="exampleInputDescription"
                placeholder="Enter the material">
            <span asp-validation-for="Material" class="text-danger"></span>
        </div>
*@
        <div class="btn-group">
            <input type="submit" value="Confirm" class="btn btn-sm btn-outline-secondary" style="background-color: #FE6C6C;"/>
            <a class="btn btn-sm btn-outline-secondary" asp-controller="Lesson" asp-action="Detail"
                asp-route-lessonid="@Model.LessonId" asp-route-courseId="@Model.CourseId">Cancel</a>
        </div>
    </form>
</div>